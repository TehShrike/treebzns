- finish the client cache
- "Create A Lead" interface
	- identify everything that needs to be an input when creating a lead - look at the schema
	- better datalist UI for selecting clients
	- basic text input
- schema/constants needs to export named export per-table, not everything in a default export

# some time

- auto-prettify
- Redirect to app after creating company
- livereload in browser
